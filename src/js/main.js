function fizz (number) {
      return (number);
};

function fizzBuzz (start, end) {
  // for (var count = start; count <= end; count++) {
  //   if (count % 15 === 0) {
  //     return ("FizzBuzz");
  //   } else if (count % 5 === 0) {
  //     return ("Buzz");
  //   } else if (count % 3 === 0) {
  //     return ("Fizz");
  //   } else {
  //     return (count)
  //   }
  // }
};

export { fizz };
